@model List<TarefasApp.Controllers.RankingViewModel>
@{
    ViewData["Title"] = "Ranking de Colaboradores";
}
<h2>Ranking de Colaboradores</h2>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Posição</th>
            <th>Nome</th>
            <th>Setor</th>
            <th>Pontos Totais</th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Count; i++)
        {
            var item = Model[i];
            string destaque = "";
            string medalha = "";
            if (i == 0) { destaque = "table-success"; medalha = "🥇"; }
            else if (i == 1) { destaque = "table-warning"; medalha = "🥈"; }
            else if (i == 2) { destaque = "table-info"; medalha = "🥉"; }
            <tr class="@destaque">
                <td>@(i+1) @medalha</td>
                <td>@item.Nome</td>
                <td>@item.Setor</td>
                <td>@item.PontosTotais</td>
            </tr>
        }
    </tbody>
</table>
